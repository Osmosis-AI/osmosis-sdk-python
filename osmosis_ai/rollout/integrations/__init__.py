"""Framework-specific rollout integrations."""

# Import integrations explicitly from their framework modules, for example:
# ``osmosis_ai.rollout.integrations.agents.strands``. Keeping this namespace
# neutral prevents one framework from becoming a dependency of every integration.
__all__: list[str] = []
