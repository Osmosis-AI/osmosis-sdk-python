[project]
name = "<your-rollout>"
description = "Placeholder rollout scaffolded by `osmosis rollout init`."
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "osmosis-ai[server]>=0.2.21",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
